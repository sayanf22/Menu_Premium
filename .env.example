# Supabase Configuration
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your_anon_key_here

# Note: Do NOT use quotes around the values
# The anon key should be the JWT token (starts with eyJ...)

# ============================================
# SUPABASE EDGE FUNCTION SECRETS (set via CLI or Dashboard)
# ============================================
# These are NOT prefixed with VITE_ as they are server-side only
# Set them in Supabase Dashboard > Edge Functions > Secrets
#
# RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxxx     (also returned to frontend for checkout)
# RAZORPAY_KEY_SECRET=your_razorpay_secret   (server-side only, never exposed)
# RAZORPAY_WEBHOOK_SECRET=your_webhook_secret
